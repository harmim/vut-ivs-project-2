# top-most EditorConfig file
root = true

# unix style line endings, trim trailing white spaces, final new line and indent with tabs for all files
[*]
end_of_line = lf
trim_trailing_whitespace = true
insert_final_newline = true
indent_style = tab
tab_width = 4
